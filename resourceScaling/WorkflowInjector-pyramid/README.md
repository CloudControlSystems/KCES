# Workflow task container builder
  To realize a simple and effective task container adapter interface, which elegantly combined
  workflow scheduler algorithm with Kubernetes native default-scheduler.